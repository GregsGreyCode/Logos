# Suspicious / Legacy / Irrelevant Files — Review Queue

> Generated: 2026-03-23
> See CLEANUP_REPORT.md for full analysis context.
> Do NOT delete anything from this list without explicit review.

Confidence levels: **HIGH** = very confident in classification | **MEDIUM** = likely but verify | **LOW** = unclear, needs deeper read

---

## Category 1: Build Artifacts (safe to remove/gitignore)

| Path | Classification | Why | Confidence | Action |
|------|---------------|-----|------------|--------|
| `hermes_agent.egg-info/` | Build artifact | Generated by setuptools `pip install -e .` — should never be committed | HIGH | Add to `.gitignore`; `git rm -r` if tracked |
| `temp_vision_images/` | Runtime artifact | Temp files generated during vision tool calls. Currently has 5+ jpg files committed. | HIGH | Add to `.gitignore`; `git rm -r` if tracked |
| `logs/` | Runtime artifact | Log output directory. Should not be tracked. | HIGH | Add to `.gitignore`; `git rm -r` if tracked |
| `venv/` | Build artifact | Python virtual environment. Should not be tracked. | HIGH | Add to `.gitignore`; `git rm -r` if tracked |
| `__pycache__/` | Build artifact | Python bytecode cache directories (exist throughout repo) | HIGH | Add `**/__pycache__/` to `.gitignore` |

---

## Category 2: Legacy Launchers / Scripts (safe to archive)

| Path | Classification | Why | Confidence | Action |
|------|---------------|-----|------------|--------|
| `hermes` (root script, 13 lines) | Legacy launcher | `from cli import main; fire.Fire(main)` — launches old `cli.py`. Production `hermes` command is `hermes_cli.main:main` via pyproject.toml. | HIGH | Archive to `archive/hermes-origin/hermes` |
| `setup-hermes.sh` | Legacy setup script | Named "Hermes Agent Setup Script"; sets up dev env. May be superseded by `scripts/install.sh`. Verify if still needed. | MEDIUM | Rename to `scripts/setup.sh` or remove if `scripts/install.sh` covers it |

---

## Category 3: Old Standalone CLI (ambiguous — verify before archiving)

| Path | Classification | Why | Confidence | Action |
|------|---------------|-----|------------|--------|
| `cli.py` | Legacy / ambiguous | Docstring: "Hermes Agent CLI - Interactive Terminal Interface". Large file. `hermes_cli/main.py` is the current production CLI. This file is imported ONLY by the root `hermes` legacy script. However — it may have features not yet in `hermes_cli`. | MEDIUM | **Deep-read before acting.** Diff capabilities vs `hermes_cli/main.py`. If fully superseded, archive to `archive/hermes-origin/cli.py`. |
| `cli-config.yaml.example` | Ambiguous | Config example file. Unclear if it matches current hermes_cli config schema or is from old `cli.py` era. | MEDIUM | Verify against `hermes_cli/config.py` schema; update or remove |

---

## Category 4: Stale Core/ Migration Copies (need sync, not deletion)

| Path | Classification | Why | Confidence | Action |
|------|---------------|-----|------------|--------|
| `core/state.py` | Stale copy | SCHEMA_VERSION 4 — root `hermes_state.py` is SCHEMA_VERSION 8. Missing runs, workspaces, eval_results tables. Used by `agents/hermes/agent.py` which is NOT production. | HIGH | **Sync from `hermes_state.py`** (copy, then verify agents/hermes/agent.py tests pass) |
| `core/toolsets.py` | Stale copy | Missing `bug_notes` toolset. Import path refers to `core.toolsets` (self-referential). | HIGH | **Sync from `toolsets.py`** (update import path to use relative import) |
| `core/model_tools.py` | Likely stale copy | Likely diverged from root `model_tools.py`. Only used by `agents/hermes/agent.py` (not production). | MEDIUM | Sync from root `model_tools.py` when Phase B begins |
| `core/toolset_distributions.py` | Likely stale copy | Copy of root `toolset_distributions.py`. Unknown diff. | MEDIUM | Sync from root when Phase B begins |
| `core/trajectory_compressor.py` | Likely stale copy | Copy of root `trajectory_compressor.py`. Unknown diff. | MEDIUM | Sync from root when Phase B begins |
| `core/minisweagent_path.py` | Likely stale copy | Copy of root `minisweagent_path.py`. Unknown diff. | MEDIUM | Sync from root when Phase B begins |
| `core/constants.py` | Identical copy | Byte-for-byte identical to `hermes_constants.py`. Safe but redundant. | HIGH | Keep as-is (canonical target for future migration) |
| `core/clock.py` | Identical copy | Byte-for-byte identical to `hermes_time.py`. Safe but redundant. | HIGH | Keep as-is (canonical target for future migration) |
| `core/utils.py` | Identical copy | Byte-for-byte identical to `utils.py`. Safe but redundant. | HIGH | Keep as-is (canonical target for future migration) |

---

## Category 5: Work-in-Progress Agent (not production — needs fixing)

| Path | Classification | Why | Confidence | Action |
|------|---------------|-----|------------|--------|
| `agents/hermes/agent.py` | WIP / broken | Refactored agent using `core.*` imports. BUT `core.*` is stale (esp. `core.state` is v4). This file is NOT wired into production (`gateway/run.py` uses `run_agent.py`). Has improved policy enforcement and workspace scoping not yet in production. | HIGH | **Do NOT use as production runtime until core/ is synced.** After sync, this becomes the path to production. |
| `agents/hermes/__init__.py` | WIP | Part of the above | HIGH | Keep (needed for the module) |
| `agents/hermes/logos-agent.yaml` | WIP | Agent descriptor YAML for the Logos agent registry. Looks correct in concept. | MEDIUM | Verify schema matches `logos/registry/catalog.py` |

---

## Category 6: Old Branding / Hermes-Origin Content

| Path | Classification | Why | Confidence | Action |
|------|---------------|-----|------------|--------|
| `landingpage/nous-logo.png` | Legacy branding | Nous Research logo — no longer relevant to project | HIGH | Remove |
| `landingpage/hermes-agent-banner.png` | Legacy branding | Old hermes-agent banner image | HIGH | Remove (replace with Logos banner if needed) |
| `AGENTS.md` | Needs update | Title: "Hermes Agent - Development Guide". Content may be accurate but branding is wrong. | HIGH | Update title/header to "Logos Platform - Development Guide" |
| `CONTRIBUTING.md` | Needs update | Title: "Contributing to Hermes Agent". Content may be accurate. | HIGH | Update title/header to "Contributing to Logos" |
| `hermes_agent.egg-info/PKG-INFO` | Stale metadata | Old package metadata. Will regenerate correctly on next build. | HIGH | Remove entire `hermes_agent.egg-info/` dir |

---

## Category 7: RL / Atropos — Verify If Active

| Path | Classification | Why | Confidence | Action |
|------|---------------|-----|------------|--------|
| `environments/` | Likely hermes-agent / RL | Contains `HermesAgentBaseEnv` (Atropos integration), `hermes_swe_env/`, `terminal_test_env/`, `web_research_env.py`, `benchmarks/`. Used for RL training. | MEDIUM | Keep if RL training is active. Move to `agents/hermes/environments/` long-term. |
| `environments/hermes_base_env.py` | hermes-agent | "HermesAgentBaseEnv -- Abstract Base Environment for Hermes-Agent + Atropos" | HIGH | Move to `agents/hermes/environments/` eventually |
| `environments/hermes_swe_env/` | hermes-agent | Hermes SWE benchmark environment | HIGH | Move to `agents/hermes/environments/` eventually |
| `datagen-config-examples/` | hermes-agent | Data generation configs for batch runs. Examples for `batch_runner.py`. | MEDIUM | Move to `agents/hermes/datagen-config-examples/` or `docs/examples/` |
| `rl_cli.py` | hermes-agent / RL | RL training CLI runner using Tinker/Atropos | MEDIUM | Move to `agents/hermes/rl_cli.py` eventually |
| `tinker-atropos/` | Submodule | `git submodule` pointing to NousResearch/tinker-atropos. Needed for RL feature. | HIGH | Keep as submodule. Irrelevant to Logos core. |
| `mini-swe-agent/` | Submodule | `git submodule` pointing to SWE-agent/mini-swe-agent. Used by terminal_tool. | HIGH | Keep as submodule. Core tool dependency. |

---

## Category 8: Experimental / Planning Docs (review and promote or close)

| Path | Classification | Why | Confidence | Action |
|------|---------------|-----|------------|--------|
| `plans/checkpoint-rollback.md` | Open plan | Implementation plan for checkpoint/rollback. `tools/checkpoint_manager.py` exists — check if plan is done or still pending. | MEDIUM | Move to `docs/project/plans/`. If implemented, close/archive. |
| `docs/project/COMPARISON.md` | Project doc | Comparison vs competing projects. May have stale hermes-agent references. | LOW | Review; update if needed |
| `docs/project/CRITIQUE.md` | Project doc | Architecture audit. May have stale references. | LOW | Review; update if needed |
| `docs/project/ILEARNT.md` | Project doc | Lessons learned. Likely fine. | LOW | Keep |
| `docs/project/onboarding_plan.md` | Project doc | Setup wizard plan. | LOW | Verify if setup wizard is implemented; update/close if done |
| `docs/honcho-integration-spec.html` | Spec doc | HTML version of the Honcho integration spec. May be auto-generated. | LOW | Keep if manually maintained; remove if auto-generated from .md |

---

## Summary Counts

| Category | Count | Priority |
|----------|-------|---------|
| Build artifacts (gitignore/remove) | 5 dirs | **Immediate** |
| Legacy launchers (archive) | 2 files | **This week** |
| Old standalone CLI (verify first) | 2 files | **This week** |
| Stale core/ copies (sync) | 6 files | **Phase B** |
| WIP agent (fix, don't delete) | 3 files | **Phase D** |
| Old branding (remove/update) | 5 files | **This week** |
| RL/Atropos content (move later) | 7 paths | **Phase E** |
| Experimental docs (review) | 6 files | **Ongoing** |
