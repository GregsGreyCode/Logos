apiVersion: v1
kind: Service
metadata:
  name: logos
  namespace: logos
spec:
  selector:
    app: logos
  ports:
    - name: http
      port: 80
      targetPort: http
    - name: api
      port: 8080
      targetPort: 8080
      nodePort: 30902
  type: NodePort
