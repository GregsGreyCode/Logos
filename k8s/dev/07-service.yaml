apiVersion: v1
kind: Service
metadata:
  name: logos
  namespace: logos-dev
spec:
  selector:
    app: logos
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30921
    - name: api
      port: 8080
      targetPort: 8080
      nodePort: 30911
  type: NodePort
