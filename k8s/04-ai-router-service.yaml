apiVersion: v1
kind: Service
metadata:
  name: ai-router
  namespace: hermes
spec:
  type: NodePort
  selector:
    app: ai-router
  ports:
    - name: http
      port: 9001
      targetPort: 9001
      nodePort: 30901  # pinned — host docker-compose services use YOUR_DOCKER_HOST_IP:30901
